// import React from 'react';
// import { FaGift } from 'react-icons/fa';

// const ClaimButton = ({ selectedUser, onClaim, lastClaimedPoints, loading }) => (
//   <div style={{ marginTop: 20 }}>
//     <button onClick={onClaim} disabled={!selectedUser || loading} className="claim-btn">
//       <FaGift style={{ marginRight: 8 }} />
//       {loading ? 'Claiming...' : 'Claim Points'}
//     </button>
//     {lastClaimedPoints !== null && (
//       <div className="claimed-points animate-claim">
//         <strong>Claimed Points:</strong> {lastClaimedPoints}
//       </div>
//     )}
//   </div>
// );

// export default ClaimButton; 